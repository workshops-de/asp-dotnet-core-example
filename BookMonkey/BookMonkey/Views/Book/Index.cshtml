@using BookMonkey.Services.Models
@model IList<Book>

@{
    ViewData["Title"] = "Index";
}

<h2>Books</h2>

<ul>
    @foreach (var book in Model)
    {
        <li><a asp-controller="Book" asp-action="Detail" asp-route-id="@book.Isbn">@book.Title</a></li>                
    }
</ul>

